# momikaru-fc-professional（業界経験者向けFC募集LP）
# 静的公開 + リード中継用の Netlify Function 1本。
# Netlify 側で「Base directory = momikaru-fc-professional」になっている前提。
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
